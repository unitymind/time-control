!!!
%html

  %head
    %title
      = yield(:title) || "Untitled"
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag "application", "smoothness/jquery-ui-1.8.11.custom", "table", "table_ui"
    = javascript_include_tag :defaults
    = csrf_meta_tag
    = yield(:head)

  %body
    #container
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}"

      - if show_title?
        %h1= yield(:title)

      = yield
    #source-code
      Исходный код проекта:
      %a{:href => 'https://github.com/unitymind/time-control', :target => '_blank' } https://github.com/unitymind/time-control
